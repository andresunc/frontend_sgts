import { ReplaceTypePipe } from './replace-type.pipe';

describe('ReplaceTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
