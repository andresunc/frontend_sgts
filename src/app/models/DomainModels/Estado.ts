export class Estado {
    
    idEstado: number;
    tipoEstado: string;

    constructor(idEstado: number, estado: string) {
        this.idEstado = idEstado;
        this.tipoEstado = estado;
    }
}